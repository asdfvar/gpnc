develop high-level abstraction for data extraction.
create mutex to handle threading std-outputs to standard out
need to work out the intercomm_create logic to interface with multiple slave processes
rename Text_file class to Parameter class
add preamble headers to things (functions, classes, etc...)
create number of DEX slave tasks equal to the number of slave tasks
